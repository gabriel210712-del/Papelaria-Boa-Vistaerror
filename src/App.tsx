import React from 'react';
import { LandingPage } from './components/LandingPage';

/**
 * Papelaria Boa Vista - Aplicação Direta (Single-Page sem roteador)
 * Não expõe rotas, sitemap ou redirecionamentos para os clientes.
 */
export default function App() {
  return <LandingPage />;
}
